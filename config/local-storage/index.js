// https://www.npmjs.com/package/nuxt-vuex-localstorage
module.exports = {
  localStorage: ['user', 'tempDocId', 'ref'] //  If not entered, “localStorage” is the default value
  // sessionStorage: ['sfoo', 'sbar'] //  If not entered, “sessionStorage” is the default value
}
